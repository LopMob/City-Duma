# API — сводное описание

Базовый URL: `http://localhost:8000`. Формат ошибок: `{"detail": "..."}`
(для `422` дополнительно массив `errors` с деталями валидации).

## Служебное

| Метод | Путь | Описание |
|---|---|---|
| GET | `/health` | Проверка работоспособности сервиса |

## Депутаты (`Deputy`) — задача #1, готово

| Метод | Путь | Описание | Ошибки |
|---|---|---|---|
| GET | `/deputies` | Список депутатов | — |
| POST | `/deputies` | Создать депутата | `422` |
| GET | `/deputies/{id}` | Получить депутата | `404` |
| PATCH | `/deputies/{id}` | Обновить депутата | `404`, `422` |
| DELETE | `/deputies/{id}` | Удалить депутата (каскадно) | `404` |

Поля: `full_name` (2–255 симв., обязательно), `party`, `election_district`,
`is_active` (по умолчанию `true`), `created_at`.

## Комиссии (`Commission`) — задача #1, готово

| Метод | Путь | Описание | Ошибки |
|---|---|---|---|
| GET | `/commissions` | Список комиссий | — |
| POST | `/commissions` | Создать комиссию | `409` (дублирующее имя), `422` |
| GET | `/commissions/{id}` | Получить комиссию | `404` |
| PATCH | `/commissions/{id}` | Обновить комиссию | `404`, `409`, `422` |
| DELETE | `/commissions/{id}` | Удалить комиссию (каскадно) | `404` |

Поля: `name` (уникально, 2–255 симв., обязательно), `description`, `created_at`.

## Членство в комиссиях (`CommissionMembership`) — задача #2, готово

| Метод | Путь | Описание | Ошибки |
|---|---|---|---|
| GET | `/commissions/{id}/members` | Состав комиссии | `404` |
| POST | `/commissions/{id}/members` | Включить депутата в комиссию | `404` (нет комиссии/депутата), `409`, `422` |
| PATCH | `/memberships/{id}` | Назначить или снять председателя | `404`, `409`, `422` |
| DELETE | `/memberships/{id}` | Исключить депутата из комиссии | `404` |

Поля: `deputy_id` (обязательно при создании), `is_chair` (по умолчанию
`false`), `joined_at` (дата включения, ставится автоматически),
`commission_id`.

Правила (`409 Conflict`):
- один депутат не может быть дважды включён в одну комиссию;
- у комиссии не может быть больше одного председателя — чтобы назначить
  нового, сначала снимите текущего (`PATCH` с `{"is_chair": false}`).

Пример:

```
curl -X POST localhost:8000/commissions/1/members \
  -H "Content-Type: application/json" -d '{"deputy_id": 1, "is_chair": true}'
```

## Заседания и посещаемость (`Meeting`, `Attendance`) — задача #3, в разработке

Эндпоинты и правила описаны в `docs/tasks/ISSUE-3-meetings-attendance.md`;
раздел заполняется автором задачи по мере реализации.
